services:
  - type: web
    name: telegram-pallet-bot
    runtime: python
    repo: https://github.com/mihnuchka5-cloud/telegram-pallet-bot
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py