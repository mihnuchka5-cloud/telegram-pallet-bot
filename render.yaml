services:
  - type: web
    name: telegram-pallet-bot
    runtime: python
    repo: https://github.com/ТВОЙ_АККАУНТ/название-репозитория
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py